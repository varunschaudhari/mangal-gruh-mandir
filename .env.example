# MongoDB root credentials (docker-compose production)
MONGO_ROOT_USER=root
MONGO_ROOT_PASS=changeme_root

# MongoDB app user credentials
MONGO_USER=mgm_app
MONGO_PASS=changeme_app

# Domain (used in nginx config)
DOMAIN=yourdomain.com
